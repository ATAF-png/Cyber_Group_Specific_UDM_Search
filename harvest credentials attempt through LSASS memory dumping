metadata.event_type = "PROCESS_LAUNCH"
re.regex(target.process.command_line, `lsass`) nocase
(
  re.regex(target.process.command_line, `MiniDump`) nocase or
  re.regex(target.process.command_line, `process dump`) nocase or
  re.regex(target.process.command_line, `procdump`) nocase or
  re.regex(target.process.command_line, `comsvcs.dll`) nocase
)
